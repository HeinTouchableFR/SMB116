{# templates/index.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Photo api{% endblock %}

{% block body %}
    <div class="centered">
        {% dump(picture) %}
        <img src="{{ absolute_url(asset(picture.filename)) }}" />
    </div>
{% endblock %}
